%h2
  Performance Grades for <br /> <a href="http://www.alexa.com" title="Alexa.com - The Web Information Company">Alexa</a> Top 100 Sites
= will_paginate @urls
%table#top100
  %thead
    %th.alexa
      US Rank
    %th
      URL
    %th.yslow
      Yslow
    %th.page_speed
      Page Speed
  %tbody
    - @urls.each do |u|
      %tr{:class=>"#{cycle('odd', 'even')} row"}
        %td.center
          = u.country_rank
        %td.url      
          = u.url_name
        %td.center
          = (u.yslow2s.find(:last) == nil) ? 'N/A' : (link_to grade(u.yslow2s.find(:last).o), u)
        %td.center
          = (u.page_speeds.find(:last) == nil) ? 'N/A' : (link_to grade(u.page_speeds.find(:last).o), u)



