%h2
  = "Front-End Performance Recordings for <br /> <a href='http://www.#{@the_url.url_name}' title='#{@the_url.url_name}'>#{@the_url.url_name}</a>"
%table{:summary => "Front End Performance Grade Recordings for #{@url_name}"}
  %thead
    %tr
      %th
        = "Date Recorded"
      %th
        Page Weight
      %th
        Overall Score
  %tbody
    - @yslow2_scores.each do |y|
      %tr
        %td
          = link_to Yslow2.readable_time(y.created_at), y
        %td
          = Yslow2.convert_to_kilobytes(y.w)
        %td
          = y.o
